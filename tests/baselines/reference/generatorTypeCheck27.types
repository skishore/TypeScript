=== tests/cases/conformance/es6/yieldExpressions/generatorTypeCheck27.ts ===
function* g(): IterableIterator<(x: string) => number> {
>g : () => IterableIterator<(x: string) => number, any, any>
>IterableIterator : IterableIterator<T, U, V>
>x : string

    yield * function* () {
>yield * function* () {        yield x => x.length;    } () : any
>function* () {        yield x => x.length;    } () : IterableIterator<(x: any) => any, any, any>
>function* () {        yield x => x.length;    } : () => IterableIterator<(x: any) => any, any, any>

        yield x => x.length;
>yield x => x.length : any
>x => x.length : (x: any) => any
>x : any
>x.length : any
>x : any
>length : any

    } ();
}
